{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug BBB Binary (Local)",
            "request": "launch",
            "program": "${workspaceFolder}/target/armv7-unknown-linux-gnueabihf/debug/bbb_led_control",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build BBB"
        }
    ]
}